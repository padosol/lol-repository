# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# Riot Games API
RIOT_API_KEY=your-riot-api-key-here

# 참고: dev 프로파일은 Docker Compose 내부 네트워크를 사용하므로
# DB/Redis/RabbitMQ 연결 정보는 application-*.yml에 하드코딩되어 있습니다.
# - PostgreSQL: postgres:5432/lol_repository (postgres/postgres)
# - RabbitMQ: rabbitmq:5672 (guest/guest)
# - Redis: redis:6379
